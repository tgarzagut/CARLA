"""
    Code already exists but, using the pygame library, this code (to be inserted) tests and prints the state of 
    each button, wheel, pedal, etc for the Logitech G920 controller.
"""
import pygame
import sys

